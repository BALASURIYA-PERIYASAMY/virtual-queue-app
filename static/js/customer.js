// QuickQ Customer Side JS

function joinQueue(shopId) {
  alert(`Joining queue for shop: ${shopId}`);
  // TODO: Call backend API (Flask + Firebase) to join queue
}

function checkStatus(bookingId) {
  alert(`Checking status for booking: ${bookingId}`);
  // TODO: Fetch status from Firebase
}
