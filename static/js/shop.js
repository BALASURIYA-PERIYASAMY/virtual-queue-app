// QuickQ Shop Owner JS

function approveBooking(bookingId) {
  alert(`Booking ${bookingId} approved ✅`);
  // TODO: Call backend API to update booking status
}

function rejectBooking(bookingId) {
  alert(`Booking ${bookingId} rejected ❌`);
  // TODO: Call backend API to update booking status
}
